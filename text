Here's a concise handoff summary you can paste into Claude CLI or another AI.

---

# Kyros AWS Deployment Progress Summary

## AWS Region

```text
ap-south-1 (Mumbai)
```

---

# IAM

## EC2 Role

Created:

```text
kyros-ec2-role
```

Attached policies:

```text
AmazonEC2ContainerRegistryReadOnly
AmazonSSMManagedInstanceCore
AmazonSSMReadOnlyAccess
```

Additionally created a custom inline policy for:

```text
ssm:GetParameter
kms:Decrypt
ecr:DescribeRegistry
```

Purpose:

* Read `/kyros/*` Parameter Store secrets
* Decrypt SecureString
* Login to ECR

---

## CI User

Created:

```text
kyros-ci
```

Access Key created.

Current attached policies:

```text
AmazonRDSFullAccess
AmazonSSMReadOnlyAccess
AmazonEC2ContainerRegistryFullAccess
```

Need verification whether deployment policy for:

```text
rds:CreateDBSnapshot
ssm:StartSession
ec2:DescribeInstances
```

exists.

---

# ECR

Repository created:

```text
kyros-backend
```

URI:

```text
091833701189.dkr.ecr.ap-south-1.amazonaws.com/kyros-backend
```

---

# RDS

Created PostgreSQL database:

```text
DB Identifier: kyros-prod
Engine: PostgreSQL
Class: db.t4g.small
Storage: 20 GB gp3
```

Estimated cost:

```text
33.28 USD/month
```

Configuration:

```text
Single AZ
No deletion protection
Self-managed credentials
```

Master username:

```text
postgres
```

Database name:

```text
kyros
```

Endpoint:

```text
kyros-prod.crk6m6yswhvr.ap-south-1.rds.amazonaws.com
```

Port:

```text
5432
```

Security Group:

```text
kyros-rds-sg
```

Attached to RDS.

---

# Database URL

Codebase uses:

```python
postgresql+asyncpg
```

Correct URL:

```text
postgresql+asyncpg://postgres:<PASSWORD>@kyros-prod.crk6m6yswhvr.ap-south-1.rds.amazonaws.com:5432/kyros?ssl=require
```

NOT:

```text
postgresql+psycopg
```

---

# Parameter Store

Not created yet.

Will create:

```text
/kyros/production/backend
```

Type:

```text
SecureString
```

KMS:

```text
alias/aws/ssm
```

---

# Backend Configuration

Backend uses:

```python
env_prefix="KYROS_"
```

Important required variables:

```text
KYROS_APP_ENV
KYROS_DATABASE_URL
KYROS_REDIS_URL

KYROS_JWT_SECRET
KYROS_OTP_SECRET

KYROS_CORS_ALLOWED_ORIGINS

KYROS_AWS_REGION
KYROS_S3_BUCKET

KYROS_AUTHKEY_API_KEY
KYROS_AUTHKEY_OTP_TEMPLATE_NAME
KYROS_AUTHKEY_SENDER_ID

KYROS_EMAIL_FROM
KYROS_ADMIN_ALERT_EMAIL
```

Production startup checks:

```text
JWT secret >= 32 chars
OTP secret >= 32 chars
No localhost in CORS
DEBUG=false
```

---

# AuthKey

Available.

Will use:

```text
KYROS_AUTHKEY_API_KEY
KYROS_AUTHKEY_OTP_TEMPLATE_NAME
KYROS_AUTHKEY_SENDER_ID
```

---

# GitHub Actions

Workflow already exists:

```text
.github/workflows/deploy-backend.yml
```

Flow:

```text
Tests
→ Build Docker image
→ Push ECR
→ Snapshot RDS
→ Alembic migration
→ Deploy EC2 containers
→ Health check
```

Uses:

```text
AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY
EC2_HOST
RDS_INSTANCE_ID
SSH_PRIVATE_KEY
```

---

# Deployment Scripts

Verified:

### deploy-phase1.sh

Uses:

```text
SSM Session Manager
No public SSH required
```

Performs:

```text
RDS snapshot
Alembic migration
Docker deploy
Health check
```

### kyros-prepare-env.sh

Reads:

```text
/kyros/production/backend
```

from Parameter Store.

Writes:

```text
/etc/kyros/backend.env
```

Then:

```text
Docker login to ECR
```

---

# Docker Architecture

Containers:

```text
redis
backend-api
celery-worker
celery-beat
caddy
```

Postgres removed from compose.

Database hosted on RDS.

---

# Still Not Done

## EC2

Need to create:

```text
Amazon Linux 2023
t3.small
20 GB gp3
```

Attach:

```text
kyros-ec2-role
```

Attach Security Group:

```text
kyros-backend-sg
```

---

## Security Group

Need to create:

```text
kyros-backend-sg
```

Inbound:

```text
80
443
```

Initially:

```text
0.0.0.0/0
```

Later restrict to Cloudflare IPs.

---

## Elastic IP

Need:

```text
Allocate Elastic IP
Associate with EC2
```

---

## Cloudflare

Already managing:

```text
kyrosclinic.com
```

Need:

```text
api.kyrosclinic.com
→ Elastic IP
→ Proxied
```

SSL:

```text
Full (Strict)
```

Need Origin CA certificate for Caddy.

---

## Database Initialization

After EC2 launch run:

```sql
CREATE EXTENSION IF NOT EXISTS pgcrypto;
CREATE EXTENSION IF NOT EXISTS pg_trgm;
CREATE EXTENSION IF NOT EXISTS btree_gin;
```

Required by migrations.

---

# Current Status

```text
IAM Roles/User      ✅
ECR Repository      ✅
RDS PostgreSQL      ✅
RDS Security Group  ✅
AuthKey             ✅

Parameter Store     ⏳
EC2                 ⏳
Backend Security SG ⏳
Elastic IP          ⏳
Cloudflare Origin   ⏳
GitHub Secrets      ⏳
Deployment          ⏳
```

The next logical step is creating the Parameter Store secret `/kyros/production/backend`, then creating the EC2 instance and `kyros-backend-sg`.
